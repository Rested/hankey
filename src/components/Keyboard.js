import React from 'react';
import { Grid, GridRow } from 'semantic-ui-react';

export default function Keyboard({stage, targetCharacter}){
    const characterPosition = 
    return (<Grid>
        <GridRow></GridRow>
    </Grid>)
}